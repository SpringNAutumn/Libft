#include "libft.h"
#include <stdio.h>

// prueba para el split

char	*ft_itoa(int n);

int	main(int argc, char *argv[])
{
	int i = 0;
	while (strtt[i] != NULL)
		printf("%s",strtt[i]);
	return (0);
}